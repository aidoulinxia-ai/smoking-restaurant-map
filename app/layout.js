export const metadata = {
  title: "喫煙可能飲食店マップ",
  description: "今、本当に吸える店が3秒で分かる。",
};

export default function RootLayout({ children }) {
  return (
    <html lang="ja">
      <body>{children}</body>
    </html>
  );
}
